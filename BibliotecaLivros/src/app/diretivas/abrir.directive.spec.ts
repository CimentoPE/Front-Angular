import { AbrirDirective } from './abrir.directive';

describe('AbrirDirective', () => {
  it('should create an instance', () => {
    const directive = new AbrirDirective();
    expect(directive).toBeTruthy();
  });
});
